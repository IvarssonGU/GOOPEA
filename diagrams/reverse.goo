enum List = Nil, Cons(Int, List);
    
fip (List, List): List
Reverse(list, acc) =
        match list {
            Nil: acc,
            Cons(x, xs): Reverse(xs, Cons(x, acc))
        };

(): List
Main = Reverse(Cons(1, Cons(2, Nil)), Nil)