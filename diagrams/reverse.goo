data List = Nil | Cons(Int, List)
    
Reverse(list, acc) =
    match list {
        Nil: acc,
        Cons(x, xs): 
            let list = Cons(x, acc) in 
            Reverse(xs, list)
    }

Main() =
    let temp = Cons(1, Nil) in
    let temp = Cons(2, temp) in
    Reverse(temp, Nil)